version: '2'

services:
  redis:
    image: redis
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    ports:
      - '6390:6379'


  app:
    build:
      context: app
      dockerfile: Dockerfile

    volumes:
      - /home/beemax/PycharmProjects/Bot/app:/data